@inject AppState AppState
@inject NavigationManager Navigator
@code {
    protected override Task OnInitializedAsync()
    {
        if (AppState.Race is null)
        {
            Navigator.NavigateTo("editeur/courses");
        }
        return base.OnInitializedAsync();
    }
}
